module.exports = {
  // root: true,
  // // This tells ESLint to load the config from the package `eslint-config-custom`
  // extends: ["custom"],
  // settings: {
  //   next: {
  //     rootDir: ["apps/*/"],
  //   },
  // },
  rules: {
    // Possible rules you might include (customize as needed)
    // Code style and formatting
    // indent: ["error", 2], // Use 2 spaces for indentation
    // quotes: ["error", "single"], // Enforce single quotes
    // semi: ["error", "always"], // Require semicolons
    // "comma-dangle": ["error", "always-multiline"], // Require trailing commas in multiline object and array literals
    // Variable naming
    // camelcase: ["error", { properties: "never" }], // Enforce camelCase naming
    // Avoid unused variables and imports
    // "no-unused-vars": "error",
    // "no-unused-expressions": "error",
    // // Enforce consistent function declarations
    // "func-style": ["error", "declaration", { allowArrowFunctions: true }],
    // // Enforce consistent linebreak style (for cross-platform compatibility)
    // "linebreak-style": ["error", "unix"],
    // // Ensure consistent return values in arrow functions
    // "consistent-return": "error",
    // Enforce a maximum line length
    // "max-len": ["error", { code: 100, ignoreComments: true }],
    // // Enforce consistent spacing before and after keywords
    // "keyword-spacing": "error",
    // // Ensure spaces inside curly braces
    // "object-curly-spacing": ["error", "always"],
    // // Enforce import sorting
    // "import/order": [
    //   "error",
    //   {
    //     groups: [
    //       ["builtin", "external"],
    //       "internal",
    //       ["parent", "sibling", "index"],
    //     ],
    //     "newlines-between": "always",
    //   },
    // ],
  },
};
